# Expense-Tracker
A simple expense tracker application to record, view and manage the personal expenses
```markdown
# Expense-Tracker

A simple expense tracker application to record, view, and manage personal expenses.

## Features

- Add, edit, and delete expense records
- Categorize expenses
- View total spending and per-category summaries
- Filter and sort expenses by date, amount, category

## Technologies

- (List the technologies used in your project, e.g.)
  - Frontend: plain HTML+CSS+JS
  - Backend: Django
  - Database: SQLite

> Replace the above with the actual stack used in this repository.

## Installation

1. Clone the repository:
   git clone https://github.com/aadith2/Expense-Tracker.git
   cd Expense-Tracker

2. Install dependencies
 
3. Run
     python manage.py runserver

Adjust commands above to match the project's actual start script.

```
